# Hostless rule and spec.defaultBackend: catch-all routing that the entry
# format cannot express — must warn instead of being dropped silently.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bugtrfhl-ing
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  defaultBackend:
    service:
      name: bugtrfhl-default
      port: {number: 80}
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bugtrfhl-svc
                port: {number: 80}
    - host: bugtrfhl.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bugtrfhl-svc
                port: {number: 80}
