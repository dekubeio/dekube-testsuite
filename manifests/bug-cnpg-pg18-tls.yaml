apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: bugcnpg18
  namespace: data
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:18.1
  storage: {size: 1Gi}
  bootstrap:
    initdb:
      database: app
      owner: app
